# Deferred: вне V1.1/V1.2 stabilization

Эти задачи не входили в стабилизацию V1.1 (завершена 2026-04-26) / V1.2 (завершена 2026-04-29) и отложены до следующего этапа.

---

## Архитектурные

**~~DREAM-фаза как этап обработки.~~** ✓ Закрыто в FrameWeaver V1.2 + DREAM Phase V1.0.  
`dream_propose()` реализован, промоция идёт через DreamCycle. Оставшийся вопрос: расширить контракт `Weaver` trait на `dream_propose()` для второго и последующих Weavers.

**Демоция SUTRA → EXPERIENCE.**  
Что делать с промоутнутым Frame, оказавшимся ошибкой. Принципиальный вопрос: истина не возвращается в опыт — это требует архитектурного решения.

**Межсистемный обмен Frame.**  
Экспорт/импорт Frame между экземплярами AXIOM через Memory Persistence. `IMPORT_WEIGHT_FACTOR=0.7` уже есть, но протокол для Frame не определён.

**Рекурсивный UnfoldFrame (depth > 1).**  
Первая реализация UnfoldFrame разворачивает только верхний уровень (анкер + непосредственные участники). Рекурсивная развёртка вложенных Frame-анкеров — отложена.

---

## Реализационные

**Live-тестирование через CLI/дашборд.**  
Стабилизация завершена, smoke-тест проходит. Разблокировано — первые реальные Frame уже кристаллизуются в EXPERIENCE.

**Второй Weaver (CausalWeaver, SpatialWeaver и др.).**  
Разблокировано после стабилизации FrameWeaver (2026-04-26). Weaver trait верифицирован на боевой реализации.

**RevokePromotion — операция демоции.**  
Связано с архитектурным вопросом демоции выше.
