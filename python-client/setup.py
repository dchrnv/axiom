"""
Setup script for axiom-python package.

For development installation:
    pip install -e .
    pip install -e ".[dev]"

For production installation:
    pip install axiom-python
"""

from setuptools import setup

# Configuration is in pyproject.toml
setup()
